[package]
name = "nixos-build-helpers"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.102"
glob = "0.3.3"
nanoserde = {version = "0.2.1", default-features = false, features = ["json"]}
